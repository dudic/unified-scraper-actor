{
  "name": "unified-scraper-actor",
  "version": "1.0.0",
  "type": "module",
  "description": "Unified Apify actor for scraping HR Cockpit and Profiling Values platforms",
  "dependencies": {
    "apify": "^3.2.6",
    "crawlee": "^3.11.5",
    "playwright": "*",
    "cheerio": "^1.0.0",
    "p-retry": "^6.2.1"
  },
  "devDependencies": {
    "@apify/eslint-config": "^0.4.0",
    "eslint": "^8.50.0"
  },
  "scripts": {
    "start": "node src/main.js",
    "lint": "eslint ./src --ext .js,.jsx",
    "lint:fix": "eslint ./src --ext .js,.jsx --fix",
    "test": "echo \"Error: oops, the actor has no tests yet, sad!\" && exit 1",
    "postinstall": "npx crawlee install-playwright-browsers"
  },
  "author": "unified-scraper-team",
  "license": "ISC"
}
